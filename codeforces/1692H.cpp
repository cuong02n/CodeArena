/*
    author : cuong2905say
    created : 10-09-2022  17:28:24    UTC: +7
*/
#include <bits/stdc++.h>

using namespace std;
int t = 0;  // test case

int A[200001];
int start[200001];

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    cin >> t;
    while (t--) {
        int n;
        cin >> n;
        for (int i = 1; i < n + 1; i++) {
            cin >> A[i];
        }
        for (int i = 1; i < n + 1; i++) {
            // start from i:
            //  pick A[i];
        }
        
    }
    system("pause");
    return 0;
}