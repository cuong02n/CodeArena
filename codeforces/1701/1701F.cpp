#include <stdio.h>
#include <set>
#include <cstdio>
#include <bits/stdc++.h>
using namespace std;
set<int> list;
int calculate(){
	return 0;
}
int main(){
	int q;// number of query
	int d; // define for a beautiful triple
	ios_base ::sync_with_stdio(false);
	cin.tie(NULL);
	return 0;
}
